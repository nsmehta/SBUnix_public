#include <stdio.h>

int main(int argc, char *argv[], char *envp[]) {
  // puts("sbush> ");
  // return 0;
	// while(1);
	printf("xxxhello!hello!\n");
	// __asm__ __volatile__ ("int $0x80");
	while(1);
	return 0;
}
